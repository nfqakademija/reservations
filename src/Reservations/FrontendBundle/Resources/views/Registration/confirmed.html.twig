{% extends "FOSUserBundle::layout.html.twig" %}

{% block title %}{{ "reservations.frontend.register"|trans }}{% endblock %}

{% block fos_user_content %}
    {% include 'ReservationsFrontendBundle:Reservations:header.html.twig' %}
    <div class="dialog">
        <div class="panel panel-default">
            <p class="panel-heading no-collapse">{{ "reservations.frontend.register"|trans }}</p>
            <div class="panel-body text-center">
                <div class="alert alert-success" role="alert">
                    {{ "reservations.frontend.dashboard.success"|trans }}.<br />
                    <a href="{{ path('bar_new') }}">{{ "reservations.frontend.create_new"|trans }}</a>
                </div>
            </div>
        </div>
    </div>
{% endblock fos_user_content %}
